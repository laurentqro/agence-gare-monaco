# Markdown Editor for Blog Articles (GitHub Issue #46)

## Tasks (in dependency order)

### Task 1: Markdown rendering helper + public article rendering (#47)
- Add `commonmarker` gem to Gemfile
- Create a `render_markdown(text)` helper method usable from views (centralized, shared by public site and admin preview)
- Replace `simple_format` with `render_markdown` in public article show view (`articles/show.html.erb`)
- All 8 locale body fields render as markdown on the public site
- Article index card truncation (150 chars) must still work — strip HTML tags from rendered markdown before truncating
- Handle nil/blank input gracefully (return empty string)
- Unit tests for the helper covering GFM features (headings, bold, italic, links, lists, quotes, tables, images)
- Integration tests for public article show verifying markdown renders as HTML
- Existing article tests must continue to pass
- STATUS: DONE

### Task 2: Admin markdown preview endpoint (#48)
- Unblocked (Task 1 complete)
- Add `POST admin/articles/preview` route
- Controller action accepts `body` parameter with raw markdown text
- Returns a Turbo Stream response updating a `#preview` target with rendered HTML
- Uses the same `render_markdown` helper as the public site
- Requires admin authentication (unauthenticated requests redirect to login)
- Controller tests for auth, markdown rendering, Turbo Stream format, blank input
- STATUS: NOT STARTED

### Task 3: Markdown editor Stimulus controller with toolbar and Write/Preview toggle (#49)
- Blocked by Task 2
- Create `markdown_editor_controller` Stimulus controller
- Write/Preview tab toggle (in-place, GitHub-style — textarea and preview swap visibility)
- Preview fetches rendered HTML from the preview endpoint via Turbo Stream
- Toolbar buttons: Bold (**), Italic (_), H2 (##), H3 (###), Link ([text](url)), Bulleted List (- ), Numbered List (1. ), Quote (> ), Image (![alt](url) placeholder)
- Toolbar wraps selected text when selected, inserts template at cursor when not
- Only the FR body field gets the editor (toolbar + tabs); other 7 locales remain plain textareas
- Toolbar buttons have `aria-label` attributes
- Add `@rails/request.js` via importmap if needed
- Admin article form integration tests: editor markup present for FR, absent for other locales
- STATUS: NOT STARTED

### Task 4: Image upload via ActiveStorage Direct Upload (#50)
- Blocked by Task 3
- Add `aws-sdk-s3` gem to Gemfile
- Configure ActiveStorage with Hetzner Object Storage (S3-compatible) for production in `config/storage.yml`
- Disk service remains for development and test
- Extend markdown_editor_controller with image upload:
  - Toolbar image button opens file picker dialog
  - Paste from clipboard triggers upload
  - Drag and drop onto textarea triggers upload
  - All use ActiveStorage DirectUpload JS class
  - Upload progress feedback (placeholder text like `![Uploading filename...]()`)
  - On success: insert `![filename](blob_url)` at cursor position
- Controller tests for Direct Upload endpoint accessibility for authenticated admins
- Uploaded images render in both admin preview and public article view
- STATUS: NOT STARTED

## Implementation Notes
- TDD required: always write failing tests first (red-green-refactor)
- No Co-Authored-By in commit messages
- Admin is French-only (force_french_locale)
- Article body is JSON column with locale keys — no schema changes needed
- Markdown gem: commonmarker (Rust-based GFM parser)
- No HTML sanitization — admin-only input is trusted
- Tailwind Typography `prose` classes already in articles/show.html.erb
- Existing test patterns in: test/controllers/admin/articles_controller_test.rb, test/controllers/articles_controller_test.rb
- `@hotwired/stimulus` already in project
- ActiveStorage already enabled (engine loaded, storage.yml exists, image_processing gem present)

## Progress Log
- Task 1 DONE (732 total tests): Added `commonmarker` gem, created `MarkdownHelper#render_markdown`, replaced `simple_format` with `render_markdown` in article show view, updated index and homepage truncation to strip HTML from rendered markdown. 13 helper unit tests + 7 integration tests added.
